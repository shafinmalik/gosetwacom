package main

import "fmt"

func main() {
	// using bubbletea now instead of gocui for cui base
	fmt.Println("Init Program")

	// Get Devices
	// Get Device button maps
	// Get Device current setups
	// Get (foreach button) applicable settings (categorize by type: slider, keymap, boolean, etc)
}
